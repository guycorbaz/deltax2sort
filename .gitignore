# Rust
/target

# Runtime debug logs (daily-rotating, written by flexi_logger)
/logs/

# Recognition model binary — generated by models/export_embedder.py, not committed
/models/embedder.onnx

# LaTeX build artifacts (manual.pdf itself is committed)
docs/*.aux
docs/*.fdb_latexmk
docs/*.fls
docs/*.idx
docs/*.ilg
docs/*.ind
docs/*.log
docs/*.out
docs/*.toc
docs/*.synctex.gz

# Local AI tooling (BMad install + Claude Code skills)
/.claude/
/_bmad/
